<Window x:Class="MvvmDronokMobilok.mvvm.views.DronView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MvvmDronokMobilok.mvvm.views"
        mc:Ignorable="d"
        Title="DronView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="7*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        <DataGrid ItemsSource="{Binding Dronok}" SelectedItem="{Binding SelectedDron}" ColumnWidth="*"/>
        <TextBox Text="{Binding SelectedDron.Nev}" FontSize="16" Grid.Row="1"  Width="300" Height="30" />

    </Grid>
</Window>
